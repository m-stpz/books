# Graph-Like Data Models

- Does you application have many one-to-many relationships? [tree-structured data]
- Does it have some relationship between records?
  - If the answer to these questions is no: document model is appropriate
  - If the answer is no: then perhaps document model isn't appropriate

- What if many-to-many relationships are very common?
  - Relational model can handle simple cases of many-to-many
  - However, as the connections get more complex, a graph data model becomes more suitable

- A graph has:
  - vertices/nodes/entities
  - edges/connections/relationships

- Examples:
  - social graphs
  - web graph
  - rail network

## Property graphs

- In this model...

...each node consists of:

- unique identifier
- set of outgoing edges
- set of incoming edges
- collection of properties (key-value pairs)

...each edge consists of:

- unique identifier
- node at which the edge starts (the tail node)
- node at which the edge ends (the head node)
- label to describe the relationship between the two nodes
- a collection of properties (key-value pairs)

- We can think of a graph store consisting of two relational tables
  - one for nodes
  - one for edges

```sql
CREATE TABLE nodes {
    node_id integer PRIMARY KEY,
    properties json
};

CREATE TABLE edges {
    edge_id interger PRIMARY KEY,
    origin_node integer REFERENCES nodes (node_id),
    destination_node integer REFERENCES nodes (node_id),
    label text,
    properties json
};

CREATE INDEX origin_edges ON edges (origin_nodes);
CREATE INDEX destination_edges on edges (destination_nodes);
```

- Graphs evolve really well and are remarkably flexible and easily extensible

### Cypher Query Language

- Cypher is a declarative query language for property graphs
- In a graph-like data structure, even though we can reproduce it (kind of) in relational dbs, Cypher is able to in 4 lines do what a relational db would take around 25

## Triple-Stores and SPARQL

- Triple-store model is equivalent to the property graph model, however, using different words to describe the same ideas
- In triple-store, information is store in:
  - subject, predicate (verb), object
    - Jim, likes, bananas
    - subject = equal to vertex/node
    - predicate:
      1. might be a key (if object is a value)
      2. might be an edge (if object is another node)
    - object:
      1. might be a value
      - lucy, age, 33 -> vertex lucy with properties {"age": 33}
      2. another node
         - subject: head node
         - object: tail node

## Where graph databases can be used

1. Fraud detection and financial crime

- allows uncovering sophisticated fraud rings

2. Real-time recommendation engines

- E-commerce and media platforms use graphs to suggest products/content

3. Knowledge graphs and generative AI (RAG)

4. Supply chain and network operations
