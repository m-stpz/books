# Introduction on Partitioning

- On chapter 5, we discussed replication (having multiple copies of the same data on different nodes)
- For very large datasets, or very high query throughput, that isn't sufficient. We need to break the data up into partitions, aka sharding
- Usually, partitions are defined in such a way that each piece of data belongs to exactly one partition
- Each partition is a small db of its own
